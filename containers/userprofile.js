import React from "react";

export default function UserProfile({ user }) {
  return (
    <div>
      <p>{user.name}</p>;<p>{user.name}</p>;<p>{user.name}</p>;
      <p>{user.name}</p>;<p>{user.name}</p>;<p>{user.name}</p>;
      <p>{user.name}</p>;<p>{user.name}</p>;<p>{user.name}</p>;
    </div>
  );
}
